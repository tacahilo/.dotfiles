# -*- mode: makefile -*-
CURDIR  := $(shell pwd)
UNAME   := $(shell uname)
ifeq ($(UNAME), Darwin)
        EMACS   := /usr/local/bin/emacs
else
        EMACS   := /usr/bin/emacs
endif
ELFiles                 := $(__ALLSRC__:%v.el=)
ELCFiles                := $(ELFiles:%.el=%.elc)
