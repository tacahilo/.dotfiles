# Set Japan-repository
options(CRAN="http://cran.md.tsukuba.ac.jp/")
options(repos="http://cran.md.tsukuba.ac.jp/")

# General
options(tab.width = 2)
options(graphics.record=TRUE)

# Quickly-Quit to type "end()"
end <- function() {
    quit(save="no")
}
